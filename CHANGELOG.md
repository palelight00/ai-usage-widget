# 変更履歴

このプロジェクトは [セマンティック バージョニング](https://semver.org/lang/ja/) に従う。

## [0.14.1] - 2026-08-26

収集と表示の挙動は変えていない。codex（openai/codex）のソースを読んで
フォールバック経路を検証した記録と、`--raw` の診断メッセージの追加だけ。

### 追加

- `--raw` のフォールバックが 0 件のとき、`.zst` 圧縮済みの rollout の有無と件数を
  出すようにした（下の「分かったこと」の切り分け用）

### 分かったこと（openai/codex のソースで確認、2026-08-26）

- **現行の codex は 7 日より古い rollout を `.zst` に圧縮する**（mtime 基準・起動時の
  バックグラウンドジョブ）。素の `.jsonl` しか読まない本スクリプトのフォールバックが
  拾えるのは**直近 1 週間ぶんだけ**で、`--raw` の「イベントなし」の主因はこれ。
  1 週間より古い値はどのみち表示価値が薄いので、.zst の展開には対応しない
- JSONL の行フォーマット・保存先・`rate_limits` の同梱は現行ソースでも変わっていない
  （`sessions/YYYY/MM/DD/rollout-*.jsonl`、`token_count` イベント、`used_percent` /
  `window_minutes` / unix 秒の `resets_at`）。**5 時間枠が JSONL 経由で来ても
  `parse_codex` はそのまま通る**
- JSONL 側の `credits.balance` は文字列型（`Option<String>`）。数値を前提にしている
  ウィジェットの金額表示は効かず、保険の「クレジットあり」になる（実物は未観測）

## [0.14.0] - 2026-08-26

### 変更

- **small も 4 バーすべて表示するようにした（0.13.0 の絞り込みを撤回）。**
  作者の端末では Claude 2 枠 + Codex 2 枠が収まることを実機で確認した。絞ると
  small で 5 時間枠の逼迫に気づけない。画面の小さい端末で溢れる場合は
  `AIUsage.js` の `SIZE.small` の余白・フォントを削る

### 分かったこと

- **5 時間枠を含む実レスポンスを採取した（2026-08-26）。**primary = 5 時間
  （18000 秒）・secondary = 週次で、2026-07 の primary = 週次・secondary = null から
  **スロットの中身が入れ替わっていた**。スロットを決め打ちせず枠の長さで判定する
  方針は正しかった。パースは 0.13.0 のまま修正なしで通った
- **Codex の `credits.has_credits` が true で返るのを初観測。**ただし balance /
  approx_* はすべて null で、中身のある値はまだ見ていない。この形だとウィジェットは
  保険の「クレジットあり」を表示する。同時に `rate_limit_reset_credits` という
  新フィールドも現れた（未使用）
- Claude 側のレスポンスにも `limits[].group` や `spend` など新キーが増えていたが、
  使っているキーは変わらず影響なし

## [0.13.0] - 2026-08-26

### 追加

- **ChatGPT（Codex）の 5 時間制限に対応した。**2026-08 に ChatGPT へ 5 時間制限が
  追加され、週次と合わせて枠が 2 つ返りうるようになった。取得側はもともと枠の長さで
  判定しているので、パース自体は変更なしで通る
- **Codex の枠の表示順を、スロット順ではなく枠の長さ順（5 時間 → 週次）にした。**
  どのスロット（primary / secondary）にどの枠が入るかは引き続き決め打ちしない。
  どちらで来ても Claude 側（5 時間 → 週次）と並びが揃う
- **small では Codex を最長の 1 枠（週次があれば週次）に絞る。**縦 158pt に
  4 バーは入らないため。週次は取り戻せない予算なので、一目で見る対象として残した。
  5 時間枠は medium / large で見る。Claude の 2 枠は従来どおり

### 注意

- 5 時間枠を含む実レスポンスはまだ `--raw` で確認できていない。想定と違う形で
  来た場合（`empty` が出る・ラベルがおかしい等）は `--raw` で実物を見て、
  `docs/internals.md` の該当欄とこの項を更新すること

## [0.12.0] - 2026-08-15

### 追加

- **large のリセット行にも日時を併記するようにした。**`リセット 3日後（08/20 13:02）`
  のように出る。相対だけだと「いつ」か分からず、日時だけだと残りが読めないので
  両方出す。medium で使っている `compactResetText()` をそのまま使い回す

### 修正

- **リセット行が `リセット ` だけになりうる穴を塞いだ。**`resets_at` はあるが
  解釈できない場合に `untilText()` が空文字を返すため、これまでは中身の無い行が
  出ていた。相対と日時のどちらかが読めればそれだけを出し、両方だめなら行ごと出さない

### 分かったこと

- **日時の併記は古い値の見分けにも効く。**API が失敗して JSONL の古い値を配っていると
  リセット時刻が過去になるが、相対表示だけでは「まもなく」としか出ず気づけなかった。
  日時が並ぶと `リセット まもなく（08/14 07:18）` となり、過去だと目で分かる

## [0.11.0] - 2026-08-15

### 追加

- **medium でもリセット日時を出すようにした。**これまで large だけだった。縦 158pt は
  増やせないので、2 列レイアウトの横幅を使い、行ラベルと % の間へ `MM/DD HH:MM` で
  挟む。small は従来どおり省略する。large は「リセットまでの時間」のままで変えていない
  （[#1](https://github.com/palelight00/ai-usage-widget/pull/1)）

### 分かったこと

- **medium の 1 列は 430pt 幅の端末でも 161pt しかない。**実機の描画を実測したところ、
  `週次 (Fable)` の行は固定分（ラベル 58pt ＋ 日時 43pt ＋ % 27pt ＋ 間隔）で 135pt を
  使う。余りは 430pt 端末で 26pt、393pt 端末で 10pt、375pt 端末で 2pt しかない
- **`scope.model.display_name` が `Fable` より長くなるとラベルが末尾から切れる**
  （1 文字あたり約 5.2pt）。`weekly_all` と `weekly_scoped` はどちらも基底ラベルが
  「週次」で、**両者を見分ける材料は `(model)` の接尾辞だけ**なので、そこが切れると
  区別がつかなくなる。表示名が長くなったらこの行の作りを見直すこと

## [0.10.0] - 2026-08-13

### 追加

- **Codex のトークンが失効したとき、`codex exec` を 1 回呼んで公式に更新させる
  ようにした。**Claude 側の `claude -p` と同じ仕組みで、`login_required` のときだけ、
  最大 1 時間に 1 回。リフレッシュトークンは自前で使わない。
  **本物の失効ではまだ検証できていない**（トークンが有効なうちは CLI を叩いても
  更新されないため）。次の失効時が最初の実地テストになる

- **Codex のトークン失効予定を `token_expires_at` に出すようにした。**`auth.json` の
  `access_token`（JWT）の `exp` を Mac 側で読む。出力に入るのは時刻だけで、
  トークン本体は外に出ない。ログにも `codex_exp=` として出る
- README のトラブルシュートに、**API が失敗して JSONL の古い値を配っている状態**の
  項目を追加した。この状態は `codex=ok` と記録されるため、これまで説明が無かった

### 分かったこと

- **Codex のトークンは約 10 日もつ**（Claude は約 8 時間）。更新するのは `codex` CLI
  だけで、**デスクトップアプリを起動していても更新されない。**寿命が長いぶん、
  切れると気づかないまま 1 週間ほど古い値を配り続ける

## [0.9.1] - 2026-08-12

Codex の API が失効しても気づけなかった問題への対応。表示と収集の挙動は
変えていないので、0.9.0 からそのまま入れ替えられる。

### 修正

- **Codex が JSONL にフォールバックしたとき `stale` を立てるようにした。**
  これまで `status` は `ok` のままで、183 時間前の値が現在値と同じ明るさで
  描画されていた（実際に発生。9pt の注意書き一行だけが手がかりだった）
- **ウィジェットは「その数値がいつのものか」に `observed_at` を優先する。**
  JSONL 経路の `fetched_at` は読んだ時刻なので、値の古さを表さない
- **ログ行に `codex_api` と `codex_age` を出すようにした。**JSONL に落ちた回も
  `codex=ok` と記録されるため、API がいつ失効したのかを事後に追えなかった

## [0.9.0] - 2026-08-05

最初の公開版。作者の環境（macOS 26 / Apple Silicon / Claude Max / Codex team）では
実運用で安定しているが、**他人の環境での導入実績がまだ無い**ため 0.9 とする。
別の環境で導入が確認できた時点で 1.0.0 にする。

**収集側は macOS 専用、表示側は iOS。**Windows / Linux では動かない。

### 収集（Mac）

- Claude は keychain の OAuth トークンで `api.anthropic.com/api/oauth/usage`
- Codex は `~/.codex/auth.json` のトークンで `chatgpt.com/backend-api/codex/usage`。
  失敗時のみ rollout JSONL にフォールバック（API → JSONL → 前回値）
- launchd で 30 分ごとに実行（`install.sh`）
- 取得に失敗した側は前回の値を残し、`status` を差し替えて `stale` を立てる
- アクセストークンが失効したら `claude -p` を 1 回だけ呼んで公式に更新させる
  （1 時間に 1 回まで。リフレッシュトークンは自前で使わない）
- Claude の追加クレジットは、未使用のとき API の `utilization` が null で返るため、
  使用量と上限から自分で算出する（0% も表示すべき情報なので消さない）

### 表示（iPhone / Scriptable）

- 共有リンク（HTTPS）→ iCloud → 端末内の控え の順に取得し、
  `generated_at` が最も新しいものを採用する
- 共有リンクは Dropbox / Google ドライブ / OneDrive / 自前サーバーに対応。
  リンクは Keychain に保存し、スクリプトには書き込まない
- リンクの入力を求めるのは困っているときだけ（未登録・登録済みだが取得できない・
  ショートカットから明示的に呼んだ）。動いているときは黙って描画する
- クレジットは Claude / Codex とも表示する。Codex は取れた材料から
  無制限 → 残りメッセージ数 → 残高 → クレジットあり の順に選ぶ
- small / medium / large でレイアウトを変える（medium は 2 列）
- 端末の言語に追従（日本語 / 英語）。README も日本語と英語がある

### 既知の制約

- 非公開エンドポイントに依存しており、予告なく壊れる
- Claude Max / Codex team 以外のプランは未検証
- Codex のクレジットは `has_credits` が常に false で実物を確認できていない
  （表示は合成データでのみ確認）
- ウィジェット拡張からは iCloud のダウンロードを起こせないため、
  共有リンクを登録しない場合は表示が古くなることがある
